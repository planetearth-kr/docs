---
slug: /commands
sidebar_position: 13
sidebar_label: 명령어
title: 명령어
description: 플래닛어스 전용 명령어 가이드입니다.
---

# 플래닛어스 전용 명령어 가이드

`<?>` - 옵션, `[ ? ]` - 필수

## 메인서버 전용

![section](/img/guide/meta/commands/commands-section-01.webp)

## 기본 명령어

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td><code>/낚시대회</code></td><td>진행 중인 낚시대회의 순위를 봅니다</td></tr>
<tr><td><code>/출석체크</code>, <code>/출첵</code></td><td>출석체크 메뉴를 엽니다</td></tr>
<tr><td><code>/전과 &lt;숫자&gt;</code></td><td>자신의 전과 기록을 확인합니다</td></tr>
<tr><td><code>/goods &lt;이름&gt;</code></td><td>전체 특산품 혹은 <code>&lt;이름&gt;</code>을 포함하는 특산품의 리스폰 상태를 검색합니다</td></tr>
<tr><td><code>/woods</code></td><td>나무 특산품들의 리스폰 시간을 검색합니다</td></tr>
<tr><td><code>/sand</code></td><td>모래 특산품들의 리스폰 시간을 검색합니다</td></tr>
<tr><td><code>/tl</code></td><td>자기 자신의 좌표를 참여 중인 채팅 채널에 전송합니다</td></tr>
<tr><td><code>/pe toggle &lt;선택&gt;</code></td><td>서버의 여러 기능을 켜고 끕니다</td></tr>
<tr><td><code>/병과</code></td><td>병과를 변경합니다. 자세한 내용은 전투 가이드의 병과 페이지를 참고하세요</td></tr>
<tr><td><code>/원격교환 [상점 타입]</code></td><td>Basic 이상 혜택으로, 스폰에 있는 교환 NPC들을 원격으로 사용합니다</td></tr>
<tr><td><code>/sb</code></td><td>스코어보드 on/off</td></tr>
<tr><td><code>/pe 토큰생성</code></td><td>에메랄드를 들고 명령어를 사용하면 자신만의 전용 토큰 아이템이 생깁니다</td></tr>
</tbody>
</table>

## 채팅 명령어

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td>채팅 맨 앞에 <code>!</code> ex) <code>!안녕하세요</code></td><td>전체채팅</td></tr>
<tr><td><code>/l</code></td><td>지역채팅</td></tr>
<tr><td><code>/tc</code></td><td>마을채팅</td></tr>
<tr><td><code>/nc</code></td><td>국가채팅</td></tr>
<tr><td><code>/ac</code></td><td>동맹채팅</td></tr>
</tbody>
</table>

## 타우니 관련

### 주민 명령어

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td><code>/res</code> 또는 <code>/res &lt;플레이어&gt;</code></td><td>플레이어 정보 확인</td></tr>
</tbody>
</table>

### 마을 명령어

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td><code>/t help</code> 또는 <code>/t ?</code></td><td>모든 마을 명령어를 확인합니다</td></tr>
<tr><td><code>/t</code> 또는 <code>/t &lt;마을이름&gt;</code></td><td>마을 정보를 확인합니다</td></tr>
<tr><td><code>/t new &lt;마을이름&gt;</code></td><td>새 마을을 설립합니다. 5000원의 돈이 지불됩니다</td></tr>
<tr><td><code>/t list</code> / <code>/t list by &lt;정렬방식&gt;</code></td><td>서버에 존재하는 마을들을 나열합니다 / <code>&lt;정렬방식&gt;</code>으로 마을들을 나열합니다</td></tr>
<tr><td><code>/t spawn</code></td><td>마을 스폰으로 이동합니다</td></tr>
<tr><td><code>/t deposit &lt;금액&gt;</code></td><td>마을 은행으로 <code>&lt;금액&gt;</code>만큼 입금합니다</td></tr>
<tr><td><code>/t online</code></td><td>접속 중인 마을원들을 확인합니다</td></tr>
<tr><td><code>/t leave</code></td><td>소속된 마을을 떠납니다</td></tr>
</tbody>
</table>

### 시장 명령어

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td><code>/t mayor</code></td><td>모든 마을 시장 명령어를 확인합니다</td></tr>
<tr><td><code>/t add &lt;플레이어&gt;</code> 또는 <code>/t invite &lt;플레이어&gt;</code></td><td>플레이어를 마을에 초대합니다</td></tr>
<tr><td><code>/t kick &lt;플레이어&gt;</code></td><td>플레이어를 마을에서 추방합니다</td></tr>
<tr><td><code>/res set mode townclaim</code>, <code>/자동클레임</code></td><td>플레이어를 자동 클레임 모드로 설정합니다</td></tr>
<tr><td><code>/res set mode clear</code>, <code>/자동클레임해제</code></td><td>플레이어의 모드를 기본 상태로 되돌립니다</td></tr>
<tr><td><code>/자동언클레임</code> / <code>/자동언클레임해제</code></td><td>서있는 청크를 자동으로 포기합니다 / 취소합니다</td></tr>
<tr><td><code>/t set spawn</code></td><td>현재 서있는 위치를 마을 스폰으로 지정합니다</td></tr>
<tr><td><code>/t set homeblock</code></td><td>현재 서있는 위치를 마을 홈블럭으로 지정합니다. 마을 스폰을 지정하기 전에 사용해야 합니다</td></tr>
<tr><td><code>/t claim</code></td><td>현재 서있는 야생 땅을 점유합니다. 20원부터 시작하여 소유한 마을 청크의 개수에 비례하여 가격이 올라갑니다</td></tr>
<tr><td><code>/t unclaim</code></td><td>현재 서있는 마을 땅을 점유 해제합니다. 점유 해제 시 야생 땅으로 돌아갑니다</td></tr>
<tr><td><code>/t withdraw &lt;금액&gt;</code></td><td>마을 은행에서 <code>&lt;금액&gt;</code>만큼의 돈을 출금합니다</td></tr>
<tr><td><code>/t rank add &lt;플레이어&gt; &lt;랭크&gt;</code></td><td>플레이어에게 <code>&lt;랭크&gt;</code> 권한을 부여합니다. 사용 가능 랭크: 부시장, 부관, 사무관</td></tr>
<tr><td><code>/t rank remove &lt;플레이어&gt; &lt;랭크&gt;</code></td><td>플레이어에게 <code>&lt;랭크&gt;</code> 권한을 박탈합니다</td></tr>
<tr><td><code>/t trust add &lt;플레이어&gt;</code></td><td><code>&lt;플레이어&gt;</code>에게 TRUST 권한을 부여합니다. TRUST 권한이 있는 유저는 마을 소속이 아니더라도 마을의 블럭 설치/파괴 및 상자 열기 권한을 가집니다. TRUST 권한은 개인 플롯 권한보다 우선시되는 가장 높은 권한입니다</td></tr>
<tr><td><code>/t trust remove &lt;플레이어&gt;</code></td><td><code>&lt;플레이어&gt;</code>의 TRUST 권한을 박탈합니다</td></tr>
<tr><td><code>/t outlaw add &lt;플레이어&gt;</code></td><td><code>&lt;플레이어&gt;</code>를 마을 무법자로 설정합니다. 무법자가 마을 땅 안에 들어올 시 자동으로 외부로 TP됩니다. 원치 않는 외부인이 마을 땅 안에서 머무를 때 사용하세요. 전쟁 시간 동안 또는 전투 상태일 때는 발동되지 않습니다. 같은 마을원을 무법자로 설정하면 자동으로 마을에서 추방됩니다</td></tr>
<tr><td><code>/t outlaw remove &lt;플레이어&gt;</code></td><td><code>&lt;플레이어&gt;</code>를 마을 무법자에서 삭제합니다</td></tr>
<tr><td><code>/t outlawlist</code></td><td>마을의 모든 무법자 목록을 확인합니다</td></tr>
<tr><td><code>/t set</code></td><td>SET 구문과 관련된 명령어들을 나열합니다</td></tr>
<tr><td><code>/t toggle</code></td><td>TOGGLE 구문과 관련된 명령어들을 나열합니다</td></tr>
<tr><td><code>/t merge &lt;마을이름&gt;</code></td><td><code>&lt;마을이름&gt;</code> 마을에게 자신의 마을과 합병할 것을 요청합니다. 수락 시 <code>&lt;마을이름&gt;</code> 마을은 해체됩니다. 명령어를 입력하는 플레이어가 시장, 부시장 랭크를 둘 다 가지고 있으면 합병할 수 없습니다</td></tr>
<tr><td><code>/lft</code></td><td>가입할 마을을 찾는 메시지를 자동으로 보내줍니다</td></tr>
<tr><td><code>/lfr</code></td><td>주민을 모집한다는 메시지를 자동으로 보내줍니다</td></tr>
<tr><td><code>/시장승계</code></td><td>마을 시장이 장기 부재할 경우, 다른 마을원들은 <code>/시장승계</code> 명령어로 시장을 승계받을 수 있습니다. 마을 시장의 미접속 25일차부터 부시장이 시장직 승계가 가능합니다. 30일차부터는 일반 마을원도 승계받을 수 있습니다. 마을에 승계받을 부시장이 없는 경우에는 일반 마을원도 25일차부터 승계받을 수 있습니다. 서버 측에서는 마을의 시장을 임의로 교체해드리지 않습니다</td></tr>
<tr><td><code>/tsethome</code></td><td>개인 마을 홈을 지정합니다. 자기 마을 안에서만 사용 가능합니다</td></tr>
<tr><td><code>/thome</code></td><td>개인 마을 홈으로 이동합니다. 자기 마을 스폰 근처 16블럭에서만 이동할 수 있습니다</td></tr>
<tr><td><code>/psethome</code>, <code>/phome</code></td><td>tsethome과 같은 역할로, 베이직 랭크 이상부터 사용 가능합니다. 베이직 랭크는 사용 시 10골드가 소모됩니다</td></tr>
</tbody>
</table>

### 국가 명령어

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td><code>/n</code> 또는 <code>/n &lt;국가이름&gt;</code></td><td>국가 정보를 확인합니다</td></tr>
<tr><td><code>/n help</code> 또는 <code>/n ?</code></td><td>모든 국가 명령어를 확인합니다</td></tr>
<tr><td><code>/n new &lt;국가이름&gt;</code></td><td>새 국가를 설립합니다. 5000원의 돈이 마을 금고에서 지불됩니다</td></tr>
<tr><td><code>/n list</code> / <code>/n list by &lt;정렬방식&gt;</code></td><td>서버에 존재하는 국가들을 나열합니다 / <code>&lt;정렬방식&gt;</code>으로 국가들을 나열합니다</td></tr>
<tr><td><code>/n list by online</code></td><td>접속 중인 국민 수가 많은 순으로 국가를 나열합니다</td></tr>
<tr><td><code>/n spawn</code></td><td>국가 스폰으로 이동합니다</td></tr>
<tr><td><code>/n deposit &lt;금액&gt;</code></td><td>국가 은행으로 <code>&lt;금액&gt;</code>만큼 입금합니다</td></tr>
<tr><td><code>/n online</code></td><td>접속 중인 국가원들을 확인합니다</td></tr>
<tr><td><code>/online</code></td><td>접속 중인 국가/동맹국의 인원을 봅니다</td></tr>
</tbody>
</table>

### 국가 왕 명령어

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td><code>/n king</code></td><td>국가 왕의 모든 명령어를 확인합니다</td></tr>
<tr><td><code>/n add &lt;마을이름&gt;</code></td><td><code>&lt;마을이름&gt;</code>의 마을을 국가로 초대합니다. 수락 시 마을이 국가로 편입됩니다</td></tr>
<tr><td><code>/n remove &lt;마을이름&gt;</code></td><td><code>&lt;마을이름&gt;</code>의 마을을 국가에서 추방합니다</td></tr>
<tr><td><code>/n set spawn</code></td><td>현재 서있는 위치를 국가 스폰으로 지정합니다. 국가 스폰은 국가 영토 내 마을 상관없이 어디든지 지정 가능하지만, 현재 스폰에서 최대 1천 블럭만 움직일 수 있습니다</td></tr>
<tr><td><code>/n set</code></td><td>SET 구문과 관련된 명령어들을 나열합니다</td></tr>
<tr><td><code>/n ally</code></td><td>국가 동맹 설정을 엽니다</td></tr>
<tr><td><code>/n enemy add &lt;국가이름&gt;</code></td><td><code>&lt;국가이름&gt;</code>을 적대합니다. <code>&lt;국가이름&gt;</code>의 국가원들의 네임태그가 빨간색으로 표시되며, 적대국 국민들은 국가의 영토에 들어올 수 없습니다</td></tr>
<tr><td><code>/n enemy remove &lt;국가이름&gt;</code></td><td><code>&lt;국가이름&gt;</code> 국가를 더 이상 적대하지 않습니다</td></tr>
<tr><td><code>/n merge &lt;국가이름&gt;</code></td><td><code>&lt;국가이름&gt;</code> 국가에게 자신의 국가와 합병할 것을 요청합니다. 수락 시 <code>&lt;국가이름&gt;</code> 국가는 해체됩니다. 새 수도와 거리가 1만 블럭 이상인 마을들은 국가에서 자동으로 추방당합니다. 명령어를 입력하는 플레이어가 왕, 부왕 랭크를 둘 다 가지고 있으면 합병할 수 없습니다</td></tr>
<tr><td><code>/n rank add &lt;플레이어&gt; &lt;랭크&gt;</code></td><td>플레이어에게 <code>&lt;랭크&gt;</code> 권한을 부여합니다. 사용 가능 랭크: 부왕, 부관, 외교관, 지휘관</td></tr>
<tr><td><code>/국가공지 [공지 메시지]</code></td><td>국가원이 들어올 때 나오는 타이틀 메시지를 설정합니다</td></tr>
<tr><td><code>/국가방송 [공지 메시지]</code></td><td>150골드를 소모하여 실시간으로 국가원들에게 타이틀 메시지를 보여줍니다</td></tr>
<tr><td><code>/국가스폰위치</code></td><td>자기 국가의 스폰 위치를 확인합니다</td></tr>
<tr><td><code>/집결</code></td><td>근처 국가원들에게 어디로 모이라고 알람을 보냅니다. 특정 계급 이상만 사용 가능합니다</td></tr>
</tbody>
</table>

### 플롯 명령어

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td><code>/plot fs &lt;가격&gt;</code></td><td>서있는 마을 청크를 <code>&lt;가격&gt;</code>에 판매합니다</td></tr>
<tr><td><code>/plot claim</code></td><td>마을원이 판매 중인 청크 위에서 입력 시 서있는 청크를 가격을 지불하고 구매합니다</td></tr>
<tr><td><code>/plot perm</code></td><td>서있는 마을 청크의 권한을 확인합니다</td></tr>
<tr><td><code>/plot perm hud</code></td><td>서있는 땅의 권한 정보를 스코어보드에 표시합니다</td></tr>
<tr><td><code>/plot set perm</code></td><td>서있는 마을 청크의 권한을 설정합니다</td></tr>
<tr><td><code>/plot clear</code></td><td>서있는 마을 청크의 모든 표지판을 제거합니다. 잠겨 있는 상자나 문 또한 언락됩니다</td></tr>
</tbody>
</table>

### 기타 명령어

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td><code>/타우니초기화 &lt;옵션&gt;</code></td><td>마을, 국가의 외교 관계와 트러스트, 잠수 유저를 관리합니다</td></tr>
<tr><td><code>/마을방송</code></td><td>마을원 전체에게 공지문을 보냅니다</td></tr>
</tbody>
</table>

## 로비서버 전용

![section](/img/guide/meta/commands/commands-section-02.webp)

<table>
<colgroup><col style={{width:'38%'}}/><col/></colgroup>
<thead><tr><th>명령어</th><th>설명</th></tr></thead>
<tbody>
<tr><td><code>/인증해제</code></td><td>디스코드 인증을 해제합니다</td></tr>
<tr><td><code>/튜토리얼</code></td><td>튜토리얼 이미지를 다시 봅니다</td></tr>
</tbody>
</table>
